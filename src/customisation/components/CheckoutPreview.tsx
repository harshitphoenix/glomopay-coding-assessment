import React from "react";
import { type Customisations } from "../types";

type CheckoutPreviewProps = {
  draft: Customisations;
};

export function CheckoutPreview({ draft }: CheckoutPreviewProps) {
  return <div>CheckoutPreview</div>;
}
